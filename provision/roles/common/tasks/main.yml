---
- name: update apt
  become: yes
  apt:
    update_cache: yes

- name: reload systemd
  become: yes
  command: systemctl daemon-reload

- name: install packages
  become: yes
  with_items: "{{ common_packages }}"
  apt:
    pkg: "{{ item.name }}"
    state: latest
