{% for item in dkim_consul_templates %}
template {
  source = "{{ consul_template_root }}/{{ dkim_consul_template_prefix }}/{{ item.filename }}"
  destination = "{{ item.destination }}"
  command = "sh -c 'chmod 600 {{ item.destination }} && service milter-opendkim restart'"
}
{% endfor %}
